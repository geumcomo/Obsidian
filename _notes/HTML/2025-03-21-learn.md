---
---

```html

<!-- 버튼 클릭시 함수 호출  -->
<button type="button" onclick="test1()">버튼 1</button>
 <!-- 체크가 되어있으면 true  -->
 
 <script>
  //한줄주석

        // 함수 정의 법: function이라는 단어를 사용함! function test(){}

        // 자바 스크립트에서는 변수를 선언할 때

        //자료형을 지정하지 않는다. (갑이 저장될때 내부적으로 자료형이 지정됨)

        let met = "Message";

        // 자료형 문자

        var su = 100;

        // var 변수 사용지정범위 애매모호

  

        function test() { //이런 함수는 호출할 때만 수행한다.

            let ss = "AAAA";

            console.log(met + "/" + ss + "/" + su);

        }
        //t1에 지정된 class들을 모두 삭제하자!

            t1.className = "";

t1.className = "selected";

                    tab1.className = "tab_cont show";
                    
for(let i=0; i<size; i++){

                console.log(a[i].checked); 

            }
 // 현재 문서에 출력하기
			document.write(str+(a1+a2));
// 현재문서 이름이 "chk" 인 모든 요소들을 검색하여 배열로 얻는다.

            let a = document.getElementsByName("chk");
            </script>
```